---
  - name: Install and Configure Tomcat with Student App Deployment
    #hosts: localhost
    #connection: local
    hosts: appstack
    gather_facts: false
    become: yes
    roles:
      - Appstack-Role
      - Cloudwatch-Role
